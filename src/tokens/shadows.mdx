---
name: Shadows
route: /tokens/Shadows
menu: Tokens
---

import { ShadowSwatches } from '../docz-components/Swatches';
import { shadows } from './shadows';

# Shadows

The shadow key contains a scale of shadows to be used with the `boxShadow` and `textShadow` props

<ShadowSwatches shadows={shadows} />
