{
  "version": 2,
  "name": "design-system",
  "scope": "hoppin",
  "builds": [
    {
      "src": "package.json",
      "use": "@now/static-build",
      "config": { "distDir": ".docz/dist" }
    }
  ],
  "routes": [{ "handle": "filesystem" }, { "src": "/(.*)", "dest": "/" }]
}
